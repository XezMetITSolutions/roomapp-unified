export async function saveMenuToIndexedDB(menu:any[]): Promise<void> { try { if (typeof window!=='undefined') { localStorage.setItem('menuCache', JSON.stringify(menu)); } } catch {} } export async function getMenuFromIndexedDB(): Promise<any[]|null> { try { if (typeof window!=='undefined') { const m = localStorage.getItem('menuCache'); return m ? JSON.parse(m) : null; } } catch {} return null; }
